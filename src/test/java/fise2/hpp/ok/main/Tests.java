package fise2.hpp.ok.main;

import org.junit.Test;

public class Tests {
    /*
    @Test
    public void write() throws IOException, URISyntaxException {
        Path outpath = Paths.get(Utils.getJARPath().toString(), "output.txt");

        Files.write(outpath, ("test" + System.lineSeparator()).getBytes()); // default : CREATE, TRUNCATE_EXISTING, WRITE
        Files.write(outpath, "test2".getBytes(), StandardOpenOption.APPEND);
    }
    */

    @Test
    public void test() {
    }
}